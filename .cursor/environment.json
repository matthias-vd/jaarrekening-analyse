{
  "name": "Jaarrekening analyse",
  "install": "sudo apt-get update && sudo apt-get install -y python3.12-venv && python3 -m venv .venv && .venv/bin/pip install --upgrade pip && .venv/bin/pip install -r requirements.txt",
  "terminals": [
    {
      "name": "flask",
      "command": "PYTHONPATH=. .venv/bin/python web/app.py",
      "description": "Flask development server for the jaarrekening-analyse web app on http://127.0.0.1:5000"
    }
  ],
  "ports": [
    { "name": "web", "port": 5000 }
  ]
}
